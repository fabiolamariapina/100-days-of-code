var isAnagram = function (s, t) {
    // Psuedocode
    // create objects from both s and t
    // loop throguh them
    // if s and t have the same characters at the same rate
    // it's an anagram
};
isAnagram("rat", "car") // false
isAnagram("anagram", "nagaram") // true 